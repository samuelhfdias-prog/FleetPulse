{
  "name": "momesso",
  "version": "1.0.0",
  "description": "Sistema de Gerenciamento de Parque de Máquinas - Agrícolas Industriais",
  "private": true,
  "scripts": {
    "install:all": "npm ci && npm --prefix api ci && npm --prefix Front-end ci",
    "start": "concurrently \"npm run start:api\" \"npm run start:web\"",
    "start:api": "npm --prefix api run start:dev",
    "start:web": "npm --prefix Front-end start",
    "build": "npm run build:api && npm run build:web",
    "build:api": "npm --prefix api run build",
    "build:web": "npm --prefix Front-end run build",
    "test": "npm run test:api && npm run test:web",
    "test:api": "npm --prefix api test -- --runInBand --passWithNoTests",
    "test:web": "npm --prefix Front-end test",
    "audit:prod": "npm --prefix api audit --omit=dev && npm --prefix Front-end audit --omit=dev"
  },
  "keywords": [
    "nestjs",
    "angular",
    "postgres",
    "multitenant",
    "machines",
    "iot"
  ],
  "author": "Momesso",
  "license": "MIT",
  "engines": {
    "node": ">=20.19 <24",
    "npm": ">=10"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
